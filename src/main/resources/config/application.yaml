spring:
  profiles:
    active: ${PROFILE}

  application:
    name: "hexletBlog"
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yaml
  mvc:
    hiddenmethod:
      filter:
        enabled: true
base-api-url: /api
jwt:
  issuer: hexlet-blog
  expireSeconds: 1800
  secretKey: "8A7M51Xb29RCloonIlI3BXUHGILuguTO"
springdoc:
  swagger-ui:
    path: /swagger.html